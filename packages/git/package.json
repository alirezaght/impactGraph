{
  "name": "@impactgraph/git",
  "version": "0.0.0",
  "private": true,
  "description": "Git CLI adapter (ADR-0007): snapshots, status, diffs via plain git plumbing with argument arrays. The only package allowed to spawn git.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@impactgraph/application": "workspace:*",
    "@impactgraph/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
