{
  "name": "@impactgraph/mcp-server",
  "version": "0.0.0",
  "private": true,
  "description": "impactgraph MCP server (PRD §21) — thin composition root exposing the shared workspace engine as MCP tools over stdio.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/main.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "start": "tsx src/main.ts"
  },
  "dependencies": {
    "@impactgraph/contracts": "workspace:*",
    "@impactgraph/domain": "workspace:*",
    "@impactgraph/persistence": "workspace:*",
    "@impactgraph/workspace-engine": "workspace:*",
    "zod": "^3.23.0",
    "zod-to-json-schema": "^3.23.0"
  },
  "devDependencies": {
    "@impactgraph/test-kit": "workspace:*",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
