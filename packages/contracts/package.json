{
  "name": "@impactgraph/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Versioned Zod schemas + generated JSON Schema for every typed boundary (ADR-0009). Standalone DTOs — no domain imports.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate:schemas": "tsx scripts/generate-schemas.ts"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@impactgraph/domain": "workspace:*",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "zod-to-json-schema": "^3.23.0"
  }
}
