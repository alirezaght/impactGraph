{
  "name": "@impactgraph/framework-adapters",
  "version": "0.0.0",
  "private": true,
  "description": "FrameworkAdapter implementations (PRD §31): enrich the code graph with architectural meaning — modules, routes, DI, jobs. Additive only; language facts are never mutated.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@impactgraph/contracts": "workspace:*",
    "@impactgraph/domain": "workspace:*",
    "@impactgraph/language-adapters": "workspace:*"
  },
  "devDependencies": {
    "@impactgraph/test-kit": "workspace:*",
    "typescript": "^5.5.0"
  }
}
